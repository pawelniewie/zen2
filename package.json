{
  "name": "zen-webpack",
  "version": "0.0.1",
  "private": true,
  "engines": {
    "node": "5.10.0",
    "npm": "3.5.0"
  },
  "scripts": {
    "postinstall": "cd client && NODE_ENV=development npm install",
    "rails-server": "echo 'visit http://localhost:3000/hello_world' && heroku local -f Procfile.dev",
    "test": "rspec"
  }
}
